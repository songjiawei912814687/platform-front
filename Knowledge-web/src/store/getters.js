const getters = {
  useAuthority: state => state.useAuthority,
  authorityMenuList: state => state.authorityMenuList,
  authorityActionList: state => state.authorityActionList,
};

export default getters
